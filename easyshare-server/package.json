{
  "name": "easyshare-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --experimental-local"
  },
  "devDependencies": {
    "wrangler": "^3.20.0"
  }
}
